<Page
    x:Class="Project_CNPM.Views.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Project_CNPM.Views">

    <Grid Background="#EEF2FF">

        <Border Width="350"
                Padding="25"
                Background="White"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center">

            <StackPanel Spacing="12">

                <TextBlock Text="📝 ĐĂNG KÝ"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>

                <TextBox x:Name="user"
                         PlaceholderText="Tên đăng nhập"/>

                <PasswordBox x:Name="pass"
                             PlaceholderText="Mật khẩu"/>

                <PasswordBox x:Name="confirm"
                             PlaceholderText="Nhập lại mật khẩu"/>

                <Button Content="Đăng ký"
                        Click="Register_Click"
                        Background="#16A34A"
                        Foreground="White"/>

                <Button Content="Quay lại đăng nhập"
                        Click="Back_Click"
                        Background="Transparent"/>

            </StackPanel>
        </Border>
    </Grid>
</Page>